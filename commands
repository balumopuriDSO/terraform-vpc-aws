#Terraform init & Apply
for dir in 00-vpc 10-sg 20-bastion 30-vpn 40-rds 50-app-alb 60-backend.tf; do (cd "$dir" && terraform init && terraform apply -auto-approve); done


#Terraform Destroy
for dir in 60-backend.tf 50-app-alb 40-rds 30-vpn 20-bastion 10-sg 00-vpc; do (cd "$dir" && terraform destroy -auto-approve); done
